export interface Color {
  id: string;
  name: string;
  price: number;
  imageUrl: string;
}

export interface CarModel {
  id: string;
  name: string;
  price: number;
  imageUrl: string;
}

export interface Accessory {
  name: string;
  price: number;
}
